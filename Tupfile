.gitignore

CFLAGS += -g
CFLAGS += -Wall
CFLAGS += -Werror

FLAGS += -D_GNU_SOURCE

LIBS += -lrt

!cc = |> gcc $(CFLAGS) -std=c99 $(FLAGS) -c %f -o %o |> %B.o

: foreach *.c |> !cc |> 
: chronos*.o main.o |> gcc %f $(LIBS) -o %o |> chronos
: chronos*.o daemon.o |> gcc %f $(LIBS) -o %o |> chronosd
